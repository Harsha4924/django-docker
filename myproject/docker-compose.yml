version: "3.9"

services:
  django:
    build: .
    container_name: django_app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    working_dir: /app
    command: ["/usr/bin/python3.10", "manage.py", "runserver", "0.0.0.0:8000"]
